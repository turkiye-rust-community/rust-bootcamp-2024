use std::fmt::{Debug, Display};

// Function
pub fn print_first(list: &impl Display) {
    println!("{}", list);
}

// Enums

pub fn aasfa() {
    let number = 4;
    print_first(&number);
}

// Perfomans
